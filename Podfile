# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'HW_Realm_Network' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HW_Realm_Network
  pod 'RealmSwift'
  pod 'Realm'
  pod 'Alamofire', '~> 5.0'
  pod 'DynamicJSON'

end
